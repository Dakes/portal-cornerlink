package net.dakes.cornerlink.neoforge;

import net.dakes.cornerlink.PortalLinking;
import net.neoforged.fml.common.Mod;

/**
 * NeoForge entrypoint. All behaviour lives in the common source set and is applied through
 * NetherPortalBlockMixin, so there is nothing to register here.
 */
@Mod(PortalLinking.MOD_ID)
public class CornerlinkNeoForge {

	public CornerlinkNeoForge() {
	}
}
