package net.dakes.cornerlink.fabric;

import net.fabricmc.api.ModInitializer;

/**
 * Fabric entrypoint. All behaviour lives in the common source set and is applied through
 * NetherPortalBlockMixin, so there is nothing to register here.
 */
public class CornerlinkFabric implements ModInitializer {

	@Override
	public void onInitialize() {
	}
}
